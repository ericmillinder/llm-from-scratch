[project]
name = "llm-training-workshop"
version = "0.1.0"
description = "Train a GPT from scratch on a MacBook"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.0",
    "huggingface-hub>=1.19.0",
    "numpy>=2.4.6",
    "torch>=2.12.0",
    "tqdm>=4.68.2",
    "matplotlib>=3.11.0",
    "tiktoken>=0.13.0",
    "safetensors>=0.8.0",
    "gguf>=0.19.0"
]
